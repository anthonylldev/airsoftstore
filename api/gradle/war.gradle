apply plugin: "war"

bootWar {
    mainClass = "com.anthonylldev.airsoftstore.AirsoftstoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
